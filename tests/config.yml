watchTargets:
  - path: 'graceful-shutdown-spring/src'
    lifeCycle:
      onStartWatch:
        - dir: "graceful-shutdown-spring"
          cmd: './gradlew bootRun'
      onChange:
        - dir: "graceful-shutdown-spring"
          cmd: './gradlew bootRun'
    option:
      interruptible: true
